//---------------------------------------------------------------------------

#ifndef QuestionH
#define QuestionH
#include "vcl.h"
#include "Test.h"

class Question
{
	private:
		UnicodeString question;
		UnicodeString answers[4];
		UnicodeString image;
		int correct;
	public:
		void SetQuestion(UnicodeString str);
		void SetAnswer(UnicodeString str, int i);
		void SetImage(UnicodeString str);
		void SetCorrect(UnicodeString str);
		int GetCorrect();
		void Show();
};

//---------------------------------------------------------------------------
#endif
